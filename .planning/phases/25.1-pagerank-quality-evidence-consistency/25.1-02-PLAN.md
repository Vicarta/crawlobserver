---
phase: 25.1-pagerank-quality-evidence-consistency
plan: 02
type: execute
wave: 2
depends_on:
  - 25.1-01
files_modified:
  - internal/cli/root.go
  - internal/cli/writer_lock.go
  - internal/cli/writer_lock_test.go
  - internal/cli/serve.go
  - internal/cli/gui.go
  - internal/cli/crawl.go
  - internal/cli/migrate.go
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/writerlock/lock.go
  - internal/writerlock/lock_unix.go
  - internal/writerlock/lock_windows.go
  - internal/writerlock/lock_test.go
  - deploy/deploy-app.sh
  - deploy/README.md
  - internal/crawler/engine.go
  - internal/crawler/engine_test.go
  - internal/crawler/manager.go
  - internal/server/deps.go
  - internal/server/handlers_admin.go
  - internal/server/handlers_delta.go
  - internal/server/handlers_delta_test.go
  - internal/server/handlers_quality.go
  - internal/server/handlers_quality_test.go
  - internal/server/server.go
  - internal/server/server_test.go
  - internal/storage/models.go
  - internal/storage/clickhouse_quality.go
  - internal/storage/clickhouse_quality_integration_test.go
  - internal/storage/clickhouse_current_snapshot.go
  - internal/storage/clickhouse_integration_test.go
  - internal/storage/clickhouse_sessions.go
  - internal/storage/schema.go
  - migrations/001_initial.sql
  - ProductFeatures.md
autonomous: true
requirements:
  - PRQ-251-01
  - QUAL-251-01
  - QUAL-251-02
  - SNAP-251-01
  - SNAP-251-02
  - API-251-01
  - TEST-251-01
must_haves:
  truths:
    - Terminal completed never precedes finalized PR evidence.
    - Quality result and findings resolve to one evaluation/evidence revision.
    - Scheduler refresh is revision-driven and replay-safe in both directions.
    - Promotion refuses missing, failed, pending, or stale evidence.
    - Historical baselines are strict predecessors and promotion cannot move the authoritative content watermark backwards.
    - Scheduler replay converges to a fixed point across scan order and restart.
    - Admin repair of the production session does not crawl or recompute PR.
  artifacts:
    - path: internal/storage/clickhouse_quality_integration_test.go
      provides: legacy import, concurrent adoption, current-pointer failure, and promotion failure/retry integration gates
    - path: internal/server/handlers_quality_test.go
      provides: quality, PageRank evidence, and Current Snapshot provenance API contract gates
---

<objective>
Consume PageRank evidence atomically through crawl finalization, Quality Gate,
Current Snapshot promotion, and the narrow repair API.
</objective>

<execution_context>
@.planning/phases/25.1-pagerank-quality-evidence-consistency/25.1-CONTEXT.md
@.planning/phases/25.1-pagerank-quality-evidence-consistency/25.1-RESEARCH.md
@.planning/phases/25.1-pagerank-quality-evidence-consistency/25.1-VALIDATION.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto" id="25.1-02-01">
  <name>Make crawl completion fail closed on PageRank evidence</name>
  <files>internal/crawler/engine.go, internal/crawler/engine_test.go, internal/crawler/manager.go</files>
  <action>Keep session non-terminal through derived analytics. Require ComputePageRankWithOptions success plus finalized evidence before completed. On PageRank/evidence failure publish completed_with_errors with explicit derivation failure, never plain completed. Move Done/activity release after finalization so safe-restart and SSE activity cannot declare the engine gone while evidence is changing. Preserve stopped/crashed semantics and test JS/non-JS success, mutation failure, evidence failure, cancellation, and ordering.</action>
  <verify><automated>go test ./internal/crawler -run 'Finalize.*PageRank|DoneAfterFinalization|Rendered.*Finalize|Static.*Finalize' -count=1</automated></verify>
  <done>Terminal visibility and activity state are both downstream of durable evidence.</done>
</task>

<task type="auto" id="25.1-02-02">
  <name>Version quality facts and gate snapshot promotion</name>
  <files>internal/storage/models.go, internal/storage/schema.go, migrations/001_initial.sql, internal/storage/clickhouse_quality.go, internal/storage/clickhouse_quality_integration_test.go, internal/server/deps.go, internal/server/handlers_quality.go, internal/server/handlers_quality_test.go, internal/storage/clickhouse_current_snapshot.go, internal/storage/clickhouse_sessions.go, internal/writerlock/*, internal/cli/writer_lock.go, internal/cli/serve.go, internal/cli/gui.go, internal/cli/crawl.go, internal/cli/migrate.go, deploy/deploy-app.sh, deploy/README.md</files>
  <action>Add append-only quality evaluation rows keyed by `(session_id, evaluation_revision)` and append-only findings keyed by the same revision. Add a separate current-quality pointer that is published only after both the evaluation and all findings are durable; readers resolve through that pointer and never mix generations. Before any new evaluation, startup/scheduler repair, or admin repair, import each existing `crawl_quality_results` row and its complete legacy findings into an immutable deterministic `legacy_import` evaluation revision, then publish its current pointer. The import is idempotent, preserves original evaluated time/score/trust/metrics/findings, records legacy source/schema provenance, and makes the stale production revision readable through the history API before replacement. Include typed source, rules/evaluator signatures, PageRank evidence revision/time/counts, chosen baseline revision, evaluated time, and stale status. Compute the deterministic evaluation revision from evaluator version, session, evidence, rules, and baseline. Replace one-way metric refresh with revision comparison and use single-flight plus compare-and-set publication so concurrent import/scheduler/admin/startup replay produces one immutable evaluation and one current pointer. Recheck the latest finalized evidence immediately before Current Snapshot initialization/promotion. Record append-only promotion attempts/events and bind the promoted snapshot metadata by compare-and-set to evaluation revision, PageRank evidence revision, rules/evaluator revision, and baseline revision. Extend `GET /api/projects/{id}/current-snapshot` and its storage model with the persisted promotion status plus bound evaluation/evidence/rules/evaluator/baseline revisions; assert that response matches current quality/evidence. Extend session deletion/retention cleanup to remove the new session-keyed evidence/evaluation/findings/pointer/action metadata without changing historical retention policy. Evaluation publication and promotion are independent idempotent recovery steps: retry/restart may reconcile a missing or failed promotion without creating another evaluation. Refuse pending, failed, missing, or mismatched evidence. Add deterministic failure injection for concurrent legacy adoption, facts-durable/current-pointer-failed retry, evaluation-success/promotion-failed retry, and CAS loss. Do not redesign snapshot copy-on-write in this phase.</action>
  <action>Additionally, select a quality baseline only from trusted full crawls strictly older than the evaluated session by `(started_at, session_id)`. Persist an authoritative full-crawl source and latest-content watermark in an append-only promotion journal. Recheck chronology in storage after server preflight, return typed `superseded` for historical full/delta replay, preserve the watermark through fold/restart, and prove repeated scheduler passes create no revisions after predecessor lineage converges. Persist immutable DeltaPlan lineage for the materialized snapshot revision, raw full source, content watermark, watermark quality revision, and raw-source quality revision. Validate the exact historical journal quality/evidence revisions on first promotion and replay, rather than mutable current pointers; reject incomplete legacy plans or changed pre-promotion lineage as `stale_delta_baseline` before overlay. Fold/trim removes the obsolete raw Delta session and heavy payload from scheduler input but retains exact immutable quality/evidence facts referenced by the live DeltaPlan. Serialize lineage capture and all candidate reads with promotion/current-snapshot mutation under one per-project lock, including admin PageRank, orphan cleanup graph delete-to-finalization, and project deletion; prove the interleaving with deterministic barriers.</action>
  <action>Because Delta overlay mutates one shared materialized session and this phase intentionally excludes copy-on-write, enforce a hard single-writer runtime boundary. Acquire one OS advisory lock in the configured shared state directory for the entire lifetime of `serve`, `gui`, `crawl`, and `migrate`; fail a second live writer before migration, scheduler startup, or storage mutation, and rely on OS release after process exit/crash rather than deleting the lock file. Make deploy default to migrations owned by `serve`; explicit migration must pass the no-FORCE active-crawl gate, stop only the app, run alone, and restart only through the same safe gate.</action>
  <verify><automated>go test ./internal/server -run 'HistoricalFullCrawlPromotion|ConcurrentNewerSnapshot|QualitySchedulerHistoricalReplay|Quality.*Evidence|Quality.*Replay|Quality.*Concurrent|Quality.*LegacyImport|Snapshot.*Evidence' -count=1 &amp;&amp; CRAWLOBSERVER_REQUIRE_CLICKHOUSE=1 go test -tags=integration ./internal/storage -run 'ProjectCurrentSnapshotSequenceSurvivesSameMillisecondCompactionAndRestart|CurrentSnapshotDeltaRetryFinalizesBindingWithoutReapplyingContent|CurrentSnapshotJournalRejectsOutOfOrderDeltaWatermark|CurrentSnapshotJournalEqualWatermarkUpdatesBindingWithoutContentMutation|LegacyCurrentSnapshotMigrationAmbiguousProvenanceFailsClosed|LatestTrustedFullCrawlSession' -count=1</automated></verify>
  <done>Quality and findings are coherent, stale states are detectable, and promotion consumes only matching evidence.</done>
</task>

<task type="auto" id="25.1-02-03">
  <name>Add admin-only idempotent quality re-evaluation</name>
  <files>internal/server/server.go, internal/server/deps.go, internal/server/handlers_quality.go, internal/server/server_test.go, ProductFeatures.md</files>
  <action>Add POST /api/sessions/{id}/quality/re-evaluate. Require admin and session access, terminal session, available storage, explicit `confirm`, a non-empty audit `reason`, and optional expected PageRank/evaluation revisions for optimistic concurrency; return 409 with the current typed provenance when an expectation is stale. Ensure legacy quality history is imported first. If no finalized evidence exists, adopt observed-existing evidence through the deterministic, mutation-safe contract from Plan 01 without recomputing PR. Evaluate synchronously and then run the independent promotion reconciliation step. Return typed `{changed, promotionChanged, result, evidence, promotion}`; repeat calls with the same evidence/rules/baseline revisions return `changed=false`, the same evaluation revision, and still repair a missing/failed promotion idempotently. GET current quality returns durable provenance/stale state, GET quality history returns immutable revisions including `legacy_import`, and a typed GET PageRank-evidence endpoint provides report/UI readback. Log only sanitized reason/source/revisions for audit. Do not change the Growth Core key contract or unrelated endpoint authorization.</action>
  <verify><automated>go test ./internal/server -run 'QualityReevaluate|SessionQuality.*Provenance' -count=1</automated></verify>
  <done>Already-completed sessions can be repaired deterministically without a new crawl, PR compute, threshold change, or DB patch.</done>
</task>

</tasks>

<verification>
- Completed write follows finalized evidence in fake-store call order.
- PR failure is completed_with_errors and quality cannot trust it.
- Improvement and deterioration both cause a new evaluation revision.
- Repeated scheduler/admin calls are idempotent.
- Evaluation/findings are append-only and the current pointer never exposes a partial generation.
- Snapshot promotion compares all bound revisions with latest finalized evidence and can retry independently.
- Current Snapshot GET returns the persisted binding and matches current quality/evidence revisions.
- Viewer and wrong-project credentials receive denial on POST.
- Missing confirmation/reason and stale expected revisions receive typed 400/409 responses.
- Legacy result/findings import is idempotent and the original stale revision remains queryable after repair.
</verification>

<success_criteria>
- The lifecycle race is structurally impossible.
- Quality is evidence-versioned, deterministic, and repairable.
- Current Snapshot cannot consume stale trust.
</success_criteria>

<output>Create 25.1-02-SUMMARY.md after execution.</output>
