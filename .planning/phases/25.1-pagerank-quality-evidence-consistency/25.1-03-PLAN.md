---
phase: 25.1-pagerank-quality-evidence-consistency
plan: 03
type: execute
wave: 3
depends_on:
  - 25.1-02
files_modified:
  - frontend/src/lib/api.js
  - frontend/src/lib/api.test.js
  - frontend/src/lib/components/ProjectPage.svelte
  - frontend/src/lib/quality-repair.js
  - frontend/src/lib/quality-repair.test.js
  - frontend/src/lib/components/PageRankTab.svelte
  - frontend/src/lib/i18n/en.json
  - ProductFeatures.md
  - CHANGELOG.md
  - deploy/README.md
  - .planning/phases/25.1-pagerank-quality-evidence-consistency/25.1-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - UI-251-01
  - TEST-251-01
  - DEPLOY-251-01
must_haves:
  truths:
    - Operators can see which evidence revision produced quality and whether it is stale.
    - Only admins can invoke quality repair.
    - Production exact-session after state reconciles 51/51 with zero=0.
    - Deployment never stops an active crawl or restarts ClickHouse.
---

<objective>
Expose evidence provenance, close regressions, and safely repair/accept the
exact production session through supported product behavior.
</objective>

<execution_context>
@.planning/phases/25.1-pagerank-quality-evidence-consistency/25.1-CONTEXT.md
@.planning/phases/25.1-pagerank-quality-evidence-consistency/25.1-RESEARCH.md
@.planning/phases/25.1-pagerank-quality-evidence-consistency/25.1-VALIDATION.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto" id="25.1-03-01">
  <name>Render Quality and PageRank provenance</name>
  <files>frontend/src/lib/api.js, frontend/src/lib/api.test.js, frontend/src/lib/components/ProjectPage.svelte, frontend/src/lib/quality-repair.js, frontend/src/lib/quality-repair.test.js, frontend/src/lib/components/PageRankTab.svelte, frontend/src/lib/i18n/en.json</files>
  <action>Add typed PageRank-evidence readback and re-evaluate client methods. In the existing Quality modal show evaluated at, immutable evaluation revision/source, evidence revision/source/status, eligible/positive/zero counts, rules/evaluator/baseline revisions, promotion status, and stale warning. Add an admin-only Re-evaluate command with explicit confirmation and required audit reason, pass the currently displayed expected evidence/evaluation revisions, handle 409 by showing and refreshing current provenance, and keep stable loading/success/error state. Route the ProjectPage repair action through a narrowly tested orchestration helper: after every successful or idempotent POST, re-read authoritative quality, immutable history, and PageRank evidence through GET, refresh Current Snapshot, and only then update modal state and the session badge; never replace complete findings with a partial POST result. On POST 409, refresh authoritative quality and badge without entering the success snapshot path. On other errors, preserve the prior complete modal/badge state and report the error. PageRank summary shows the same evidence revision, predicate version, and eligible count. Keep the work-focused dense layout and existing authorization/UI patterns.</action>
  <verify><automated>npm --prefix frontend test -- --run &amp;&amp; npm --prefix frontend run lint</automated></verify>
  <done>Visible report and Quality Gate values carry comparable provenance and admins can repair stale quality.</done>
</task>

<task type="auto" id="25.1-03-02">
  <name>Run full regression and document functionality</name>
  <files>ProductFeatures.md, CHANGELOG.md, deploy/README.md, .planning/phases/25.1-pagerank-quality-evidence-consistency/25.1-VERIFICATION.md</files>
  <action>Update canonical product catalog, changelog, and safe rollout notes. Run targeted mutation/concurrency/replay tests, full Go suite, frontend tests, lint, production build, and diff checks. Exercise desktop/mobile Playwright locally or against production after deploy. Record every acceptance assertion and residual risk in VERIFICATION.</action>
  <verify><automated>git diff --check &amp;&amp; go test ./... -count=1 &amp;&amp; npm --prefix frontend test -- --run &amp;&amp; npm --prefix frontend run lint &amp;&amp; npm --prefix frontend run build</automated></verify>
  <done>All relevant regressions pass and documentation matches behavior.</done>
</task>

<task type="auto" id="25.1-03-03">
  <name>Commit, push, deploy safely, and accept exact production session</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/25.1-pagerank-quality-evidence-consistency/25.1-VERIFICATION.md</files>
  <action>Create an intentional Phase 25.1 commit without unrelated worktree changes and push the current codex branch. Before production mutation capture a sanitized rollback checkpoint: current app image/version, Compose state, health, Current Snapshot pointer/provenance, exact-session stale evaluation revision, and a metadata-only backup of the affected quality/evidence/promotion tables; record the prior image rollback command without executing it. Run CHECK_ONLY and stop if any crawl is active. Build only app, run CHECK_ONLY again, restart only app through restart-app-safe.sh without FORCE, verify /api/health, Compose state, ClickHouse SELECT 1/continuity, and logs. Through authenticated supported API POST re-evaluate exact session cecabb70-b621-48a1-9dc4-1feb3c3757cb with confirmation, reason, and expected revisions; do not recompute PageRank. Then GET quality, PageRank evidence, PageRank report, and Current Snapshot provenance. Prove the legacy 51/51 FINAL state was adopted once as deterministic `observed_existing`, the stale 20-zero evaluation remains queryable as immutable history, the new evaluation has the same 51 eligible/51 positive/zero=0 population, legitimate crawl_config_changed warning remains, and promotion consumes only the new bound revision. Repeat the same POST and prove `changed=false` while an absent/failed promotion can still reconcile without duplicating evaluation or promotion events. Without closing the modal, verify the idempotent POST is followed by authoritative GET refresh and the legitimate crawl_config_changed finding plus session badge remain visible; closing/reopening must show the same state. Verify strict-older baseline lineage and a monotonic full/Delta Current Snapshot content watermark, then wait through at least two scheduler intervals and prove no new evaluation, promotion, or snapshot revision is published after convergence. Verify Quality modal in desktop/mobile. Do not create/delete/reassign a crawl or manually mutate historical page/session/quality rows. Mark phase complete only after evidence passes; if acceptance fails, restore only the prior app image and metadata pointer from the captured checkpoint through the safe app-only gate.</action>
  <verify><automated>git diff --check &amp;&amp; git status --short</automated></verify>
  <done>Commit is pushed, production app is healthy, ClickHouse was not restarted, and exact-session API/UI values are consistent.</done>
</task>

</tasks>

<verification>
- No active crawl at either production safety gate.
- No FORCE, whole-stack restart, session deletion, PageRank recompute, or threshold edit.
- Exact session PageRank and Quality share evidence revision and counts.
- Legacy adoption is deterministic, the old stale evaluation remains queryable,
  and repeated repair is unchanged while promotion recovery remains idempotent.
- Historical full/Delta replay is `superseded`; strictly older baseline lineage
  and the authoritative content watermark remain stable across scheduler ticks.
- `crawl_config_changed` remains visible.
- Recent app logs contain no unexpected errors.
- Rollback checkpoint names the prior app image, metadata backup, and snapshot pointer.
</verification>

<success_criteria>
- Operators can diagnose and repair stale quality through the product.
- Production contradiction is eliminated with auditable evidence.
- Phase 25.1 is committed, pushed, verified, and Phase 26 remains unstarted.
</success_criteria>

<output>Finalize 25.1-VERIFICATION.md and mark requirements/roadmap/state complete.</output>
