---
phase: 25.1-pagerank-quality-evidence-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/storage/models.go
  - internal/storage/schema.go
  - internal/storage/clickhouse_pages.go
  - internal/storage/clickhouse_quality.go
  - internal/storage/clickhouse_integration_test.go
  - internal/storage/clickhouse_pagerank_evidence_test.go
  - internal/storage/pagerank_test.go
  - migrations/001_initial.sql
  - ProductFeatures.md
autonomous: true
requirements:
  - PRQ-251-02
  - PRQ-251-03
  - TEST-251-01
must_haves:
  truths:
    - PageRank reports, evidence, and quality zero-top metrics use one eligible set.
    - Successful PR mutation is verified through FINAL before finalized evidence is published.
    - Failed computations create failed evidence and never masquerade as finalized.
    - Existing completed sessions can acquire audited observed evidence without recomputation.
  artifacts:
    - path: internal/storage/clickhouse_pages.go
      provides: shared predicate, PageRank compute verification, evidence readback
    - path: internal/storage/schema.go
      provides: additive PageRank and quality lineage schema
    - path: internal/storage/clickhouse_pagerank_evidence_test.go
      provides: mutation, predicate, revision, concurrency, and legacy integration gates
---

<objective>
Create the storage-level source of truth that all PageRank reports and quality
decisions can reference by revision.
</objective>

<execution_context>
@.planning/phases/25.1-pagerank-quality-evidence-consistency/25.1-CONTEXT.md
@.planning/phases/25.1-pagerank-quality-evidence-consistency/25.1-RESEARCH.md
@.planning/phases/25.1-pagerank-quality-evidence-consistency/25.1-VALIDATION.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto" id="25.1-01-00">
  <name>Reconcile shared dirty files before editing</name>
  <files>All plan files</files>
  <action>Snapshot and inspect existing diffs in every target. Preserve the already-present terminal-order and heuristic-refresh changes as input, but replace heuristic behavior with the versioned contract. Do not reset, stash, checkout, or overwrite unrelated Phase 26/previous work.</action>
  <verify><automated>git diff --check</automated></verify>
  <done>Only Phase 25.1 hunks are added and all prior local work remains intact.</done>
</task>

<task type="auto" id="25.1-01-01">
  <name>Add typed PageRank evidence and additive schema</name>
  <files>internal/storage/models.go, internal/storage/schema.go, migrations/001_initial.sql, internal/storage/clickhouse_pages.go, internal/storage/clickhouse_integration_test.go</files>
  <action>Add append-only PageRank evidence events with attempt UUID, predecessor, started/finalized/failed state, source, algorithm/predicate versions, deterministic options signature, graph/rank fingerprint, graph/eligible/positive/zero counts, query identity, timestamps, and sanitized failure. Stamp computed page rows with revision. A started event precedes mutation; ready follows FINAL verification that eligible rows have the revision and positive rank; errors publish failed evidence. Observed-existing adoption derives deterministic identity from session, versions, options, and fingerprint, is single-flight/CAS guarded, rejects active mutation, double-reads the fingerprint, and never updates pages or sessions. Extend the shared integration `testStore` helper so unavailable ClickHouse calls `t.Fatal` when `CRAWLOBSERVER_REQUIRE_CLICKHOUSE=1` and preserves skip behavior otherwise.</action>
  <verify><automated>CRAWLOBSERVER_REQUIRE_CLICKHOUSE=1 go test -tags=integration ./internal/storage -run 'PageRankEvidence|LegacyEvidenceAdoption' -count=1</automated></verify>
  <done>Every new compute has auditable success/failure evidence and legacy sessions can be observed without recomputation.</done>
</task>

<task type="auto" id="25.1-01-02">
  <name>Unify eligible-page predicates and report provenance</name>
  <files>internal/storage/clickhouse_pages.go, internal/storage/clickhouse_quality.go, internal/storage/models.go, internal/storage/pagerank_test.go, internal/storage/clickhouse_pagerank_evidence_test.go</files>
  <action>Define `pagerank-eligible-v1`: HTML, status 200-299, no followed redirect, canonical empty/self/equal URL; include noindex; exclude redirects, canonical aliases, non-2xx HTML, and non-HTML. Never include pagerank>0 in eligibility. Reuse it in top/distribution/treemap totals, evidence, TopPageRankURLs, quality zero-top, and overlap. Return evidence plus eligible/positive/zero totals, use FINAL, and assert eligible=positive+zero including exact 51/51.</action>
  <verify><automated>go test ./internal/storage -run 'PageRank' -count=1 &amp;&amp; CRAWLOBSERVER_REQUIRE_CLICKHOUSE=1 go test -tags=integration ./internal/storage -run 'PageRankQualityPopulation|PageRankEvidence' -count=1</automated></verify>
  <done>The same session/revision/predicate produces identical population counts in reports and quality.</done>
</task>

<task type="auto" id="25.1-01-03">
  <name>Document the evidence model</name>
  <files>ProductFeatures.md</files>
  <action>Update the canonical feature catalog with finalized/failed/adopted PageRank evidence, shared eligibility, and provenance semantics in the same change set.</action>
  <verify><automated>git diff --check</automated></verify>
  <done>ProductFeatures matches the storage behavior before downstream code consumes it.</done>
</task>

</tasks>

<verification>
- Mutation success without evidence is an error.
- Evidence success without FINAL count verification is impossible.
- Failed evidence cannot be selected as latest finalized.
- Observed-existing evidence is deterministic, double-read verified, SELECT/INSERT only, and never PageRank UPDATE.
- Report and quality integration fixtures reconcile eligible=positive+zero.
- Required integration runs fail, never skip, when ClickHouse is unavailable.
</verification>

<success_criteria>
- PageRank has a durable revision contract.
- All PageRank quality/report population logic is shared.
- Storage integration gates pass without historical rewrites.
</success_criteria>

<output>Create 25.1-01-SUMMARY.md after execution.</output>
