---
phase: 25.3-raw-stabilized-sitemap-retry-suppression
plan: 02
status: complete
completed: 2026-08-28
---

# Plan 25.3-02 Summary

## Delivered

- Daily Delta planning now projects the exact storage-derived stability proof
  through the existing project URL normalizer into Selector v2 while holding the
  same Current Snapshot planning lock used for lineage validation.
- Launched Delta metadata persists all v2 counts, proof lineage/digest, source
  provenance, and the publication hold. Preview mirrors the three new top-level
  count fields only for a v2 selector; legacy responses retain their old shape.
- Quality-plan metrics preserve the v2 published/actionable/stable distinction
  and proof provenance for audit readback.
- Conditional requests remain bound to Current Snapshot validators. Raw
  stability does not create a validator overlay and therefore cannot produce a
  `304` against a representation different from the published snapshot.
- Daily Delta UI now shows published differences, actionable refetches,
  raw-stable acknowledgement, canaries, deferral, proof pair/digest/status, and
  the explicit Current Snapshot hold banner while retaining the legacy view.

## Verification

- `go test ./internal/server -run 'Delta.*Stable|Delta.*Publication|Delta.*Preview' -count=1`
- `go test ./internal/storage -run 'Delta.*Stability|Delta.*Publication' -count=1`
- `go test -race ./internal/server ./internal/storage -run 'Delta.*(Stable|Publication|Preview)' -count=1`
- `npm --prefix frontend test -- --run src/lib/components/DeltaCrawlTab.test.js`
- `npm --prefix frontend run lint`
- Frontend Prettier check and `git diff --check`

No live crawl, rescan, or production action was performed in this plan.
