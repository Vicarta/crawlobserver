---
phase: 25.3-raw-stabilized-sitemap-retry-suppression
plan: 01
status: complete
completed: 2026-08-28
---

# Plan 25.3-01 Summary

## Delivered

- Added a bounded immutable-evidence projection for the newest two completed,
  project-bound Fresh sitemap observations. A stable URL must have the same
  normalized identity, valid `lastmod`, terminal page evidence, and equal
  nonzero content hash in both observations.
- Added deterministic Selector v2 classification: every Fresh-versus-Published
  difference is either actionable or raw-stable acknowledged. Stable evidence
  is excluded from selected, deferred, and canary work.
- Added v2 persisted provenance for counts, proof lineage/digest, source map,
  and an explicit `publication_held` state.
- Kept the materialized Current Snapshot as the sole publication authority.
  Any v2 stable acknowledgement fails closed for sitemap publication, even if a
  malformed caller clears the boolean hold flag.
- Updated `ProductFeatures.md` with the execution-only stability contract.

## Verification

- Focused storage stability and Current Snapshot publication tests passed.
- Selector/config compatibility tests passed, including legacy v1 decoding and
  a stable-only v2 publication hold.
- No live crawl, rescan, historical data rewrite, or production action was
  performed in this plan.
