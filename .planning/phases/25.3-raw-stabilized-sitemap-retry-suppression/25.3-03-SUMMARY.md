---
phase: 25.3-raw-stabilized-sitemap-retry-suppression
plan: 03
status: passed
completed: 2026-08-28
---

# Phase 25.3 Plan 03 Summary

## Delivered

- Completed the Phase 25.3 regression, documentation, and app-only rollout for
  raw-stabilized sitemap retry suppression.
- Implementation and ProductFeatures changes are in pushed commit
  `448534e335eb1bd7ddd307347304ecde59259c6e` on
  `codex/cleanup-deployed-worktree`.
- The production overlay contains only the reviewed Phase 25.3 runtime files;
  production's intentionally absent project-bound targeted-rescan route was
  not introduced.

## Verification

- `go test ./... -count=1`: passed.
- `go test -race ./internal/server ./internal/storage ./internal/config -run 'Delta.*(Stable|Launch|Reservation|Scope|Precision|Publication|Preview)|TestDeltaSitemap' -count=1`: passed.
- `go build ./...`: passed.
- `go vet ./internal/storage ./internal/config ./internal/server`: passed.
- `npm --prefix frontend test -- --run`: 122/122 passed.
- `npm --prefix frontend run lint`: passed.
- `npm --prefix frontend run build`: passed with existing Svelte/a11y and bundle-size warnings.
- Frontend Prettier check for the three owned files: passed.
- `git diff --check` and staged diff check: passed.
- Non-printing staged secret scan: passed.
- Required ClickHouse-tagged integration fixtures were not runnable locally:
  the Docker/Colima daemon was unavailable at the local socket. No production
  ClickHouse fixture mutation was used and this limitation is not represented
  as a passing integration result.

## Production Acceptance

- Two no-force active-crawl gates passed before and after the app overlay;
  both reported no running or queued crawl sessions.
- Only `crawlobserver-app` was recreated through `restart-app-safe.sh`; the
  ClickHouse container was not restarted.
- Final loopback health was `{"status":"ok"}`. The app is running with image
  `sha256:c34b163a5f00180567906f9510049e63adf7483f75d5f595e8e8b8084db860ec`.
- ClickHouse remained healthy and authenticated `SELECT 1` returned `1`.
- The final read-only app log check found zero matching panic/fatal/exception/
  error lines in the last 100 app log lines.
- Read-only DI Preview (`project_id=
  5c527919-c565-4a85-a446-25e2945bce75`) returned `sitemap_refresh.mode=fresh`,
  2,088 fresh/raw rows, 2,680 published-snapshot rows, 4 added, 596 removed,
  and 0 invalid entries. Its current snapshot baseline is
  `2aa4424f-fe2c-460e-8dc0-fdbd494f5079`, revision 1.
- Preview selection returned 2,063 published differences split into 13
  actionable and 2,050 raw-stable acknowledgements, with proof pair
  `bef21482-4513-486d-8999-1374f25daa9e` →
  `636f285a-743a-4c4d-aca3-cde03eaf08f9` and proof digest
  `a0d3b9f54c1c4954a1ab2d13b8300bea5404f971bfc2d2505aeaec3d6ba80b3a`.
  The partition invariant holds: `2,063 = 13 + 2,050`.
- Preview selected/launched 38 URLs: 13 actionable events plus 25 rotating
  canaries, with 0 deferred and a complete selection. Publication remains
  explicitly held because raw stability is not publication evidence. No other
  source contributed a nonzero candidate in this preview.
- No live crawl, rescan, publication, cleanup, or historical data rewrite was
  performed.

## Rollback and Operator Notes

- Rollback archive: `/opt/crawlobserver/app/.deploy-backups/phase253-448534e/runtime-before.tar.gz`.
- Rollback archive SHA-256:
  `dff371f8bc2b7f7845db11065491ac6bc078aa90906b49aece8ccdd6d5532413`.
- The archive overlay briefly copied a restrictive top-level mode onto the app
  directory; it was immediately repaired to mode 755 before restart. No data
  or service state was lost.
- One verification wrapper initially placed `CHECK_ONLY=1` before `sudo`, so
  the safe script recreated the app once more without force or active crawls.
  The correct `sudo env CHECK_ONLY=1 FORCE=0` gate then passed and final health
  remained green.
- Compose reported the pre-existing `mirofish-frontend` orphan warning; no
  orphan removal was requested or performed.
