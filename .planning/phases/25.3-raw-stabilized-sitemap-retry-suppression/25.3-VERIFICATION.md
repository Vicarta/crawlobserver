---
phase: 25.3
slug: raw-stabilized-sitemap-retry-suppression
status: passed
verified: 2026-08-28
production_deployed: true
---

# Phase 25.3 Verification

## Result

Phase 25.3 implementation, local regression gates, reviewed app-only
deployment, and read-only DI acceptance completed. Raw stability suppresses
redundant execution only; Current Snapshot remains the publication and quality
authority.

## Contract Evidence

- Selector v2 requires two distinct ordered completed fresh observations bound
  to one project, normalized URL, valid lastmod, terminal page evidence, and
  equal nonzero content hash.
- Missing, failed, partial, invalid, cross-project, zero-hash, changed-hash,
  and newly-forward evidence remains actionable. Stability does not become a
  canary or deferred event.
- Preview and persisted plans carry published-difference, actionable,
  stable-acknowledged, selected, deferred, canary, source, proof-pair, and
  proof-digest provenance. Publication is held whenever raw-stable differences
  exist.
- Conditional validators remain bound to the published Current Snapshot; raw
  observations cannot supply a validator overlay.
- The atomic manual/scheduler launch reservation rechecks the same snapshot,
  proof, URL partition, and active-session state and rolls back its reservation
  on a failed receipt mark.

## Automated Gates

- Full Go tests, focused race tests, build, changed-package vet, frontend
  Vitest (122/122), lint, frontend build, Prettier, diff checks, and the
  non-printing staged secret scan passed.
- The repository-wide unrelated `go vet ./...` copylock warning at
  `internal/updater/updater_test.go:191` remains pre-existing and the file was
  not changed.
- ClickHouse integration-tag fixtures could not run because the local Docker
  daemon was unavailable. This is recorded as an environmental limitation;
  no production fixture or historical data mutation was substituted.

## Production Acceptance

- Deployment source commit: `448534e335eb1bd7ddd307347304ecde59259c6e`.
- Production app image: `sha256:c34b163a5f00180567906f9510049e63adf7483f75d5f595e8e8b8084db860ec`.
- Both no-force active-crawl safety gates passed. Only the app was recreated;
  ClickHouse stayed on its existing healthy container.
- `/api/health` returned `{"status":"ok"}`; ClickHouse `SELECT 1` returned
  `1`; the final 100-line app-log error scan returned zero matches.
- DI project `5c527919-c565-4a85-a446-25e2945bce75` read-only Preview:

  | Field | Value |
  | --- | --- |
  | Refresh | `fresh` at `2026-08-28T14:52:15.635828922Z` |
  | Fresh/raw sitemap rows | `2,088 / 2,088` |
  | Published snapshot rows | `2,680` |
  | Added / removed / invalid | `4 / 596 / 0` |
  | Published snapshot session / revision | `2aa4424f-fe2c-460e-8dc0-fdbd494f5079 / 1` |
  | Published differences | `2,063` |
  | Actionable / stable acknowledged | `13 / 2,050` |
  | Proof pair | `bef21482-4513-486d-8999-1374f25daa9e` → `636f285a-743a-4c4d-aca3-cde03eaf08f9` |
  | Proof digest | `a0d3b9f54c1c4954a1ab2d13b8300bea5404f971bfc2d2505aeaec3d6ba80b3a` |
  | Event selected / canaries / deferred | `13 / 25 / 0` |
  | Selected/launched | `38 / 38` |
  | Publication state | held: raw-stable sitemap differences are not publication evidence |

- Acceptance invariants passed: `2,063 = 13 + 2,050`; selected `38 = 13 +
  25`; and the launched set is exactly the deduplicated 13-event plus
  25-canary partition. The 13 unresolved/pending sitemap events remain
  actionable; the large bulk-reset cohort is represented as stable
  acknowledgement and is not actionable solely due to the published lag.
- The preview was read-only and created no crawl session. No live crawl,
  rescan, publication, cleanup, or historical rewrite was performed.

## Rollback

- Pre-deploy archive:
  `/opt/crawlobserver/app/.deploy-backups/phase253-448534e/runtime-before.tar.gz`
- SHA-256:
  `dff371f8bc2b7f7845db11065491ac6bc078aa90906b49aece8ccdd6d5532413`

The tracked implementation is pushed on `codex/cleanup-deployed-worktree`; the
phase documentation closeout is the only remaining repository action.
