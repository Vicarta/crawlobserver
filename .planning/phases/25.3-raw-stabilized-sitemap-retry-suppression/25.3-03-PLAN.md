---
phase: 25.3-raw-stabilized-sitemap-retry-suppression
plan: 03
type: execute
wave: 3
depends_on: [25.3-01, 25.3-02]
files_modified:
  - internal/storage/clickhouse_delta_test.go
  - internal/server/delta_sitemap_selection_test.go
  - internal/server/handlers_delta_test.go
  - internal/storage/clickhouse_current_snapshot_test.go
  - frontend/src/lib/components/DeltaCrawlTab.test.js
  - ProductFeatures.md
  - .planning/phases/25.3-raw-stabilized-sitemap-retry-suppression/25.3-VERIFICATION.md
autonomous: true
requirements: [TEST-253-01, DOC-253-01, DEPLOY-253-01]
---

<objective>
Close regression and documentation gates, obtain independent review, then
deploy app-only and prove the DI Preview no longer treats stable bulk metadata
as ~2k network refetches.
</objective>

<tasks>

<task type="auto" id="25.3-03-01">
  <name>Complete regression matrix</name>
  <files>owned tests and ProductFeatures.md</files>
  <action>Cover the full validation matrix, including DI-shaped legacy pair, rejected proof variants, selector determinism, storage publication hold, concurrent manual/scheduler replay, Current Snapshot-bound conditional requests, malformed/legacy API and UI states. Run focused race, full Go, changed-package vet/build, frontend tests/lint/build, and diff checks.</action>
  <verify><automated>go test ./... -count=1 &amp;&amp; go build ./... &amp;&amp; npm --prefix frontend test -- --run &amp;&amp; npm --prefix frontend run lint &amp;&amp; npm --prefix frontend run build &amp;&amp; git diff --check</automated></verify>
</task>

<task type="auto" id="25.3-03-02">
  <name>Commit, push, and deploy safely</name>
  <files>Phase 25.3 owned files only</files>
  <action>After independent review, commit and push. On production run CHECK_ONLY; if any crawl is active, wait without force or stop. Build only app, repeat CHECK_ONLY, restart only app through the safe script, verify health, ClickHouse continuity/SELECT 1, and logs. Do not start crawl/rescan.</action>
  <verify><automated>Record the read-only DI Current Snapshot ID/revision, proof-pair IDs/digest, exact PublishedDifferenceTotal, ActionableTotal, StableAcknowledgedTotal, and assert Published = Actionable + Stable. Verify the audited missing-coverage URL remains actionable; WillLaunch equals the deduplicated actionable + other sources + canaries partition; and no unexplained bulk-reset cohort remains actionable. A changed production term must fail or be explicitly reconciled, never silently pass.</automated></verify>
</task>

</tasks>

<output>Create 25.3-03-SUMMARY.md and 25.3-VERIFICATION.md after execution.</output>
