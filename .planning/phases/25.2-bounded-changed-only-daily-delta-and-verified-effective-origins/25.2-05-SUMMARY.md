---
phase: 25.2-bounded-changed-only-daily-delta-and-verified-effective-origins
plan: 05
status: passed
completed: 2026-08-28
---

# Phase 25.2 Plan 05 Summary

## Implemented

- Added fail-closed response-only `effective_origin` and
  `effective_origin_state` enrichment for session list and detail responses.
- Normalized launched URL identities for evidence lookup while preserving exact
  raw `SeedURLs`; partial or conflicting response proof remains unavailable or
  ambiguous instead of falling back to seed, sitemap, canonical, or config.
- Bounded effective-origin enrichment to the requested session page and batches
  of 50 sessions.
- Updated Project Sessions to show the proven operational origin and separately
  labelled raw seed, including long-value wrapping and neutral unavailable or
  ambiguous states.
- Updated `ProductFeatures.md` with the complete Phase 25.2 contract.

## Verification

- Affected backend packages: passed.
- `go test ./... -count=1`: passed.
- Focused `go test -race`: passed.
- `go vet` for all changed packages: passed.
- `go build ./...`: passed.
- Frontend Vitest: 121/121 passed.
- Frontend ESLint and production build: passed.
- `git diff --check`: passed.
- Independent final review: passed with no production-blocking finding.

Repository-wide `go vet ./...` still reports the pre-existing unrelated
`internal/updater/updater_test.go:191` copylock warning. Required ClickHouse
fixture tests could not run locally because no Docker/ClickHouse daemon is
available; production acceptance is limited to health, `SELECT 1`, logs, and
read-only API/UI checks and will not run fixture mutations against production.

## Production Acceptance

- Pushed implementation commit
  `fc5a708afe0e3cc2a6cda63a42f80737a5e22ccc` on
  `codex/cleanup-deployed-worktree`.
- Waited for active DI Daily Delta session
  `636f285a-743a-4c4d-aca3-cde03eaf08f9` to finish normally; it was never
  stopped or forced.
- Passed the zero-active-crawl safety gate before and after the app-only image
  build, then restarted only the app through `restart-app-safe.sh` without
  `FORCE=1`.
- Deployed artifact
  `b2aefa98c78a2ee913bb4820898617bdb6d7ef36b2470b01f06e5ff45d3fbb6b`
  as app image
  `sha256:16a7c25a36b59ef669283b17157e1382a1460eed024d1054a19b62a4a3251e24`.
- Verified app health `ok`, existing ClickHouse container continuity and
  `SELECT 1`, clean recent app logs, and the exact deploy marker.
- Existing Chrome read-only acceptance confirmed the new Daily Delta controls
  and provenance. DI currently has 2,063 forward-lastmod events plus 25
  rotating canaries, so its next nearly full run is evidence-driven; the 5,000
  candidate value remains a ceiling, not a minimum or target.
- Project Sessions shows proven `https://www.diskinternals.com` origins with
  the raw seed separately. Historical Daily Delta sessions without complete
  launched/final-response proof correctly remain `Origin unavailable` rather
  than treating their HTTP seed as an operational origin.
- Production is not a Git checkout and lacks the earlier undeployed
  project-bound targeted-rescan runtime. The rollout therefore used a reviewed
  54-file Phase 25.2 overlay that preserved that production base rather than
  silently expanding this deployment's scope.

## Safety

No live crawl, rescan, PageRank recompute, session reassignment, or historical
data rewrite was used for acceptance. Old unused `/tmp/crawlobserver-*`
artifacts (109 MB, all older than seven days and unused by any process) were
removed after verification; app health remained `ok`.
