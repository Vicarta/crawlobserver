---
phase: 25.2-bounded-changed-only-daily-delta-and-verified-effective-origins
plan: 02
status: complete
completed: 2026-08-26
---

# Plan 25.2-02 Summary

## Delivered

- Added bounded ClickHouse reads for the dual sitemap terms: the exact
  materialized Current Snapshot sitemap is P, while R is the newest terminal
  completed Phase 21 fresh Delta observation with the trusted raw full source
  as fallback. Failed and synthetic sessions are excluded; raw remains retry
  provenance rather than a publication permission.
- Wired the shared changed-only selector under the existing project promotion
  lock for Preview, Run now, and the scheduler. Fresh sitemap candidates now
  consist only of selected events and canaries, with raw/published lineage,
  selector revision, counts, source provenance, and completeness persisted in
  each DeltaPlan.
- Added preview/UI visibility for sitemap event, pending-unpublished, canary,
  deferred, and held-publication state. Deferred sitemap events emit a
  non-blocking quality finding and metrics without being marked consumed.
- Added a fail-closed sitemap publication preflight before all Current Snapshot
  mutations. It rechecks the exact materialized binding, fresh observation, and
  selector revision. Complete selections may replace sitemap evidence;
  incomplete selections can overlay selected pages but retain the published
  sitemap term.
- Updated `ProductFeatures.md` with the dual-term gate and incomplete-selection
  publication behavior.

## Verification

- `go test ./internal/server ./internal/storage -count=1`
- `npm --prefix frontend test -- --run DeltaCrawlTab`
- `npm --prefix frontend run lint`
- `git diff --check`

The required ClickHouse fixture was added. Its required-harness invocation was
attempted but the local ClickHouse endpoint at `localhost:19000` was not
running, so that integration execution could not run in this workspace. No
live crawl, deployment, or production mutation was performed.
