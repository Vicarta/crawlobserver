---
phase: 25.2-bounded-changed-only-daily-delta-and-verified-effective-origins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/fetcher/sitemap.go
  - internal/fetcher/unit_test.go
  - internal/apikeys/store.go
  - internal/apikeys/store_test.go
  - internal/config/config.go
  - internal/config/sitemap_config_test.go
  - internal/server/delta_sitemap_selection.go
  - internal/server/delta_sitemap_selection_test.go
  - ProductFeatures.md
autonomous: true
requirements:
  - DELTA-252-01
  - DELTA-252-02
  - DELTA-252-03
  - COMPAT-252-01
  - TEST-252-01
must_haves:
  truths:
    - The published sitemap observation, not the newest raw observation, controls whether an event remains pending.
    - Only added URLs and strictly forward valid lastmod values are change events.
    - Default sitemap selection prioritizes evidence-backed events plus up to 50 deterministic rotating canaries, subject to the existing changed/new and global safety ceilings.
    - Legacy settings/config decode safely and explicit saved limits are not overwritten.
  artifacts:
    - path: internal/server/delta_sitemap_selection.go
      provides: pure dual-term selector, selection provenance, and stable canary rank
    - path: internal/fetcher/sitemap.go
      provides: shared strict W3C lastmod parser/comparator for selection and later Phase 26 reuse
    - path: internal/apikeys/store.go
      provides: bounded Delta selection defaults and SQLite persistence
  key_links:
    - from: internal/fetcher/sitemap.go
      to: internal/server/delta_sitemap_selection.go
      via: conservative precision-aware forward-lastmod comparison
    - from: internal/apikeys/store.go
      to: internal/config/config.go
      via: settings become immutable DeltaPlan selection provenance
---

<objective>
Define and prove the pure changed-only sitemap selection contract before it is
wired to storage, preview, run, scheduler, or promotion.
</objective>

<execution_context>
@.planning/phases/25.2-bounded-changed-only-daily-delta-and-verified-effective-origins/25.2-CONTEXT.md
@.planning/phases/25.2-bounded-changed-only-daily-delta-and-verified-effective-origins/25.2-RESEARCH.md
@.planning/phases/25.2-bounded-changed-only-daily-delta-and-verified-effective-origins/25.2-VALIDATION.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto" id="25.2-01-01">
  <name>Specify strict lastmod and dual-term event behavior</name>
  <files>internal/fetcher/sitemap.go, internal/fetcher/unit_test.go, internal/server/delta_sitemap_selection.go, internal/server/delta_sitemap_selection_test.go</files>
  <action>Add table-driven failing tests and a pure selector. Preserve raw lastmod and accept only supported W3C date/date-time values. Compare date/date and mixed precision by UTC calendar date and datetime/datetime by UTC instant. Classify added and strictly forward events relative to published P. Compare the same URL with newest complete raw R only to label `newly_observed` versus `pending_unpublished`; never let R suppress an event still ahead of P. Missing/invalid/equal/backward values are unchanged. Reuse/export the strict fetcher parser so Phase 26 can extend it rather than create a conflicting parser.</action>
  <verify><automated>go test ./internal/fetcher ./internal/server -run 'LastMod|DeltaSitemapSelection' -count=1</automated></verify>
  <done>Pure tests prove that an event remains pending after an untrusted raw observation and only forward evidence creates a change.</done>
</task>

<task type="auto" id="25.2-01-02">
  <name>Add bounded settings and persisted selection contract</name>
  <files>internal/apikeys/store.go, internal/apikeys/store_test.go, internal/config/config.go, internal/config/sitemap_config_test.go</files>
  <action>Add idempotent SQLite settings and JSON-compatible DeltaPlan fields for sitemap changed limit (`0` means all evidence-backed events), canary count (default 50), raw observation session/time, published current session/snapshot revision/content watermark, selector revision, UTC rotation epoch, event total/selected/deferred, canary selected, selection-complete, and per-URL selection source. Preserve the existing changed/new/global configurable maximums as safety ceilings rather than lowering them into daily targets. Sanitize negative values but preserve zero semantics. Keep old settings rows and old session configs readable with typed legacy/absent provenance.</action>
  <verify><automated>go test ./internal/apikeys ./internal/config -run 'Delta|SitemapSelection|Legacy' -count=1</automated></verify>
  <done>Preview/run can carry one auditable bounded selection contract without destructive migration or legacy decode failures.</done>
</task>

<task type="auto" id="25.2-01-03">
  <name>Implement deterministic event and canary bounds</name>
  <files>internal/server/delta_sitemap_selection.go, internal/server/delta_sitemap_selection_test.go, ProductFeatures.md</files>
  <action>Select events before canaries, with stable identity ordering for events and stable hash rank over project ID, published snapshot revision, persisted UTC epoch, and URL for unchanged canaries. Exclude every event URL from canaries, deduplicate normalized identities, emit exact selected/deferred/source counts, and mark selection incomplete whenever any published-ahead event is deferred. Test 0/1/30/31 events, fewer/exactly/more than 50 canaries, same-epoch repeat, next-epoch rotation, duplicates, and pending-unpublished priority. Document changed-only selection and bounds in ProductFeatures.</action>
  <verify><automated>go test ./internal/server -run 'DeltaSitemapSelection' -count=20 &amp;&amp; git diff --check</automated></verify>
  <done>The selector is deterministic within an epoch, rotates canaries across epochs, and cannot hide deferred published-ahead events.</done>
</task>

</tasks>

<verification>
- `go test ./internal/fetcher ./internal/apikeys ./internal/config ./internal/server -run 'LastMod|DeltaSitemapSelection|DeltaSettings' -count=1`
- Same project/P/epoch input produces byte-equivalent ordered output.
- Raw R changes provenance only; P controls pending state.
</verification>

<success_criteria>
- Changed-only candidate semantics are executable, bounded, deterministic, and backward compatible.
- Downstream plans receive explicit selection-completeness and lineage fields.
</success_criteria>

<output>Create 25.2-01-SUMMARY.md after execution.</output>
