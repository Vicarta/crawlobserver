---
phase: 25.5-passwordless-email-authentication-with-resend-and-migrated-users
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/apikeys/store.go
  - internal/apikeys/users.go
  - internal/apikeys/users_test.go
  - internal/apikeys/middleware.go
  - internal/server/handlers_auth.go
  - internal/server/server.go
  - internal/server/ratelimit.go
  - internal/server/auth_email.go
  - internal/server/auth_email_test.go
  - internal/server/server_test.go
  - frontend/src/lib/api.js
  - frontend/src/lib/api.test.js
  - frontend/src/lib/components/LoginPage.svelte
  - frontend/src/lib/components/LoginPage.test.js
  - frontend/src/lib/components/UserManagement.svelte
  - frontend/src/lib/components/UserManagement.test.js
  - deploy/docker-compose.yml
  - deploy/.env.example
  - deploy/config.production.example.yaml
  - ProductFeatures.md
autonomous: true
requirements:
  - AUTH-255-01
  - INVITE-255-01
  - LOGIN-255-01
  - SESSION-255-01
  - RESEND-255-01
  - UI-255-01
  - COMPAT-255-01
  - TEST-255-01
  - DOC-255-01
  - DEPLOY-255-01
must_haves:
  truths:
    - "Existing users migrate by invitation without losing roles, project assignments, or valid sessions."
    - "Password login is unavailable; verified email codes are single-use for 15 minutes."
    - "Invitation acceptance and email login create 14-day cookie sessions."
    - "Resend secrets stay server-side and tests send no live email."
    - "API-key and project authorization behavior remains unchanged."
  artifacts:
    - path: "internal/apikeys/users.go"
      provides: "Durable email identity, invitation, challenge, and session lifecycle"
    - path: "internal/server/handlers_auth.go"
      provides: "Passwordless public and administrator auth contracts"
    - path: "internal/server/auth_email.go"
      provides: "Bounded Resend email delivery adapter"
    - path: "frontend/src/lib/components/LoginPage.svelte"
      provides: "Email/code and invitation access UI"
    - path: "ProductFeatures.md"
      provides: "Canonical passwordless authentication behavior"
  key_links:
    - from: "frontend/src/lib/components/LoginPage.svelte"
      to: "internal/server/handlers_auth.go"
      via: "request-code, verify-code, invitation inspection and acceptance"
    - from: "internal/server/handlers_auth.go"
      to: "internal/apikeys/users.go"
      via: "single-use invitation/challenge storage transactions"
    - from: "internal/server/handlers_auth.go"
      to: "internal/server/auth_email.go"
      via: "fakeable invitation and login-code delivery"
---

<objective>
Replace local password login with a complete passwordless Resend flow, migrate
existing users safely through invitations, prove the security and compatibility
contract, and deploy only when the external prerequisites and existing rollout
safety gates are satisfied.
</objective>

<execution_context>
@.planning/phases/25.5-passwordless-email-authentication-with-resend-and-migrated-users/25.5-CONTEXT.md
@.planning/phases/25.5-passwordless-email-authentication-with-resend-and-migrated-users/25.5-RESEARCH.md
@.planning/phases/25.5-passwordless-email-authentication-with-resend-and-migrated-users/25.5-VALIDATION.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto" id="25.5-01-01">
  <name>Add durable passwordless identity and token lifecycles</name>
  <files>internal/apikeys/store.go, internal/apikeys/users.go, internal/apikeys/users_test.go</files>
  <action>Extend SQLite users with normalized unique email identity while preserving legacy rows, roles, project assignments, API keys, and active sessions. Add hashed seven-day invitations and hashed six-digit login challenges with a fifteen-minute TTL, replacement semantics, five-attempt limit, and transactional single-use consumption. Keep the legacy password columns only as inert schema compatibility fields; no password verifier may remain in the interactive login path.</action>
  <verify><automated>go test ./internal/apikeys -run 'Test(User|Invitation|LoginCode|Session)' -count=1</automated></verify>
  <done>Existing identities can be invited without replacement, and invitation/code replay, expiry, collision, and cross-user cases fail closed.</done>
</task>

<task type="auto" id="25.5-01-02">
  <name>Add bounded Resend delivery and passwordless HTTP contracts</name>
  <files>internal/config/config.go, internal/config/config_test.go, internal/apikeys/middleware.go, internal/server/handlers_auth.go, internal/server/server.go, internal/server/ratelimit.go, internal/server/auth_email.go, internal/server/auth_email_test.go, internal/server/server_test.go</files>
  <action>Add server-only Resend configuration and a timeout-bound sender using the official HTTPS API and idempotency header. Replace the password login route with generic request-code and verify-code routes, public invitation inspection/acceptance, and administrator invite/create/update operations. Apply the auth limiter, set fourteen-day HttpOnly SameSite=Lax cookies with Secure derived from the public HTTPS origin, preserve logout and existing valid sessions, and never expose account existence, codes, tokens, or provider secrets.</action>
  <verify><automated>go test -race ./internal/apikeys ./internal/config ./internal/server -run 'Test.*(Auth|Invitation|LoginCode|Resend|Session|User)' -count=1</automated></verify>
  <done>The HTTP contract is passwordless, provider calls are fakeable, public enumeration is prevented, and API-key/project authorization behavior is unchanged.</done>
</task>

<task type="auto" id="25.5-01-03">
  <name>Replace login and user administration UI</name>
  <files>frontend/src/lib/api.js, frontend/src/lib/api.test.js, frontend/src/lib/components/LoginPage.svelte, frontend/src/lib/components/LoginPage.test.js, frontend/src/lib/components/UserManagement.svelte, frontend/src/lib/components/UserManagement.test.js</files>
  <action>Replace username/password controls with email request, six-digit code verification, resend, and invitation acceptance states. Change user administration to email, role, project assignments, and invite/resend status; remove password inputs and old login calls. Cover loading, invalid, expired, used, rate-limited, and provider-failure behavior without disclosing whether an arbitrary email exists.</action>
  <verify><automated>npm --prefix frontend test -- --run &amp;&amp; npm --prefix frontend run check &amp;&amp; npm --prefix frontend run build</automated></verify>
  <done>An invited user can accept access and later sign in by code, while no interactive password UI or request remains.</done>
</task>

<task type="auto" id="25.5-01-04">
  <name>Document, verify, commit, and conditionally deploy</name>
  <files>deploy/docker-compose.yml, deploy/.env.example, deploy/config.production.example.yaml, ProductFeatures.md, .planning/phases/25.5-passwordless-email-authentication-with-resend-and-migrated-users/25.5-VALIDATION.md</files>
  <action>Document the Resend key, verified sender, public HTTPS origin, seven-day invitation, fifteen-minute code, fourteen-day cookie, migration, and rollback prerequisites. Update ProductFeatures.md in the same change. Run the focused and repository-required Go/frontend gates plus diff review, fix only Phase 25.5 findings, then commit and push. Deploy only if the Resend credentials, verified sender, and a valid current administrator session are present; if they are, use two no-force active-crawl gates and restart only the app. Missing prerequisites must be recorded as a deployment blocker and must not restore password login.</action>
  <verify><automated>go test ./... &amp;&amp; go vet ./... &amp;&amp; go build ./cmd/crawlserver &amp;&amp; npm --prefix frontend test -- --run &amp;&amp; npm --prefix frontend run check &amp;&amp; npm --prefix frontend run build &amp;&amp; git diff --check</automated></verify>
  <done>The implementation and documentation are pushed, validation evidence is recorded, and production is either safely accepted or explicitly blocked on named external prerequisites.</done>
</task>

</tasks>

<verification>
- Existing user IDs, roles, projects, API keys, and unexpired sessions survive the migration.
- Invitation expiry is exactly seven days, login-code expiry is exactly fifteen minutes, and successful cookie sessions expire after fourteen days.
- Password login, password controls, and config-admin fallback are absent from the interactive auth flow.
- Resend is exercised only through a fake or local HTTP fixture in tests; no live email or crawl is triggered.
- Missing production prerequisites block deployment without weakening authentication.
</verification>

<success_criteria>
- Administrators can migrate an existing account by assigning its email and issuing an invitation.
- Invited users can accept once and subsequently authenticate using a single-use email code.
- Authorization semantics outside interactive login are unchanged and fully regression-tested.
- ProductFeatures.md and deployment examples describe the exact shipped contract.
</success_criteria>

<output>After completion, create `25.5-01-SUMMARY.md` and `25.5-VERIFICATION.md` with test, commit, push, migration, and deployment evidence.</output>
