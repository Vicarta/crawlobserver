# Phase 25.4 Plan 01 Summary

## Implemented

- Unknown authenticated GET paths at `/api` or below now return the stable JSON
  error `404 {"error":"API endpoint not found"}` before the SPA fallback.
- Missing and invalid credentials on API paths return typed 401 JSON without a
  browser Basic-auth challenge. Browser routes retain their prior challenge and
  SPA history fallback behavior.
- The exact Current Snapshot route and public health endpoint remain unchanged.
- `ProductFeatures.md` records the fail-closed API namespace contract.

## Verification Before Rollout

- Focused tests and focused race tests: passed.
- `go test ./...`: passed.
- `go vet ./internal/apikeys ./internal/server`: passed.
- `go build ./...`: passed.
- Frontend: 122/122 tests, lint, and production build passed; existing Svelte
  accessibility/reactivity and bundle-size warnings remain unchanged.
- `go vet ./...` remains blocked by the pre-existing unrelated
  `internal/updater/updater_test.go` copy-lock finding; it was not modified.
- Strict GSD plan check: passed.
- Independent Sol code review: one test-strength finding fixed, final PASS.

## Scope

No SEO Dashboard, Delta, crawler, storage, session, or production data changes.
No live crawl or rescan was invoked.
