---
phase: 25.4-fail-closed-unknown-api-routing-for-integrations
status: passed
verified_at: 2026-09-02
implementation_commit: a5441d3
---

# Phase 25.4 Verification

## Result

PASS. Unknown authenticated API GET paths no longer resolve to the embedded SPA,
API authentication failures have a JSON media type, and the existing Current
Snapshot and health contracts remain intact.

## Automated Evidence

- Focused contract tests: passed.
- Focused race tests for `internal/apikeys` and `internal/server`: passed.
- `go test ./...`: passed.
- `go vet ./internal/apikeys ./internal/server`: passed.
- `go build ./...`: passed.
- Frontend tests: 122/122 passed; lint and production build passed with existing
  Svelte accessibility/reactivity and bundle-size warnings.
- `git diff --check`: passed.
- Strict Sol plan check: PASS.
- Independent Sol code review found one test-strength gap; the full typed
  Current Snapshot response is now compared to the fixture. Final review: PASS.
- Repository-wide `go vet ./...` remains blocked only by the pre-existing
  unrelated `internal/updater/updater_test.go` copy-lock finding.

## Production Rollout

- First `CHECK_ONLY=1` no-force gate: no active crawl sessions.
- Preserved rollback image:
  `crawlobserver:rollback-phase254-a5441d3` =
  `sha256:de6cbed15e640e82102187a1494ef5b35d83cec4c788983eb0a59f21bca21903`.
- The production source checkout is intentionally non-Git and older than the
  branch. A first build attempt exposed that drift before image export or
  restart; the two source files were restored from backup. The exact Phase 25.4
  hunk was then applied to the production versions and the app image built
  successfully, without importing unrelated branch changes.
- Second `CHECK_ONLY=1` no-force gate: no active crawl sessions.
- The safe script recreated only `crawlobserver-app`.
- New app image:
  `sha256:3c7829acfae1f71ad9af8972d708f2e16fde8b225e49f6c5bef6b221163e5378`.
- App container changed from `b7cdf5f669af...` to `b5834e7616a6...`.
- ClickHouse container remained `6b9921598b94...`, healthy; `SELECT 1` returned
  `1`.

## Production API Acceptance

- `GET /api/health`: `200 application/json`, `status=ok`.
- Authenticated
  `GET /api/projects/5c527919-c565-4a85-a446-25e2945bce75/current-snapshot`:
  `200 application/json`, `current_session_id=2aa4424f-fe2c-460e-8dc0-fdbd494f5079`.
- Authenticated trailing-slash/unknown variant: `404 application/json`,
  `error=API endpoint not found`.
- Unauthenticated exact Current Snapshot request: `401 application/json`,
  `error=unauthorized`, no `WWW-Authenticate` browser challenge.
- Recent app log scan: zero `panic`, `fatal`, or `error` lines.

## Safety

No live crawl, rescan, delete, recompute, publication, migration, ClickHouse
restart, or historical-data mutation was performed.
