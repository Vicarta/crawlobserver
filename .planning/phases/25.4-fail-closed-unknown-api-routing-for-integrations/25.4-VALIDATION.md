# Phase 25.4 Validation Contract

## Automated Gates

- `go test ./internal/apikeys ./internal/server -run 'TestAuthenticate(NoCredentials|InvalidAPIKey)|TestBuildHandler_BasicAuthOnly|TestProjectCurrentSnapshotRouteReturnsJSON|TestUnknownAPIRoutesDoNotFallThroughToSPA|TestUnknownFrontendRouteStillFallsThroughToSPA|TestAuth_Health' -count=1`
- `go test ./...`
- `go vet ./...`
- Frontend test, lint, and production build commands defined by the repository.
- `git diff --check`

## Production Acceptance

After two no-force active-crawl safety checks and an app-only restart:

- authenticated exact Current Snapshot: `200 application/json`, parseable body
  whose `project_id` and `current_session_id` match the requested fixture;
- authenticated unknown `/api` and `/api/...` GET: `404 application/json` with
  `{"error":"API endpoint not found"}`;
- unauthenticated exact API request: `401 application/json` with
  `{"error":"unauthorized"}` and no `WWW-Authenticate` challenge;
- `/api/health`: `200 application/json` with `{"status":"ok"}`;
- ClickHouse container healthy and `SELECT 1` succeeds;
- recent app logs contain no unexpected errors.

No production request may start, resume, delete, recompute, publish, or rescan.
